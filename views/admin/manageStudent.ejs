<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Students</title>
    <link rel="stylesheet" href="/css/dash.css">
</head>
<body>
    <div class="container">
        <!-- Navbar and Sidebar can be included similarly to the dashboard page -->
        
        <!-- Main Content -->
        <div class="main-content">
            <h1>Manage Students</h1>

            <table>
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Name</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% students.forEach(student => { %>
                        <tr>
                            <td><%= student.id %></td>
                            <td><%= student.name %></td>
                            <td>
                                <a href="/admin/updateStudent/<%= student.id %>">Update</a>
                                <a href="/admin/deleteStudent/<%= student.id %>" onclick="return confirm('Are you sure you want to delete this student?')">Delete</a>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>
